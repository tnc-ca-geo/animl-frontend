import { styled } from '../theme/stitches.config';

export const CheckboxWrapper = styled('div', {
  paddingTop: '$1',
  paddingBottom: '$2',
  'label': {
    display: 'flex',
    alignItems: 'center',
  }
});
